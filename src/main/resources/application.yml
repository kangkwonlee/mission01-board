server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://ec2-3-39-187-150.ap-northeast-2.compute.amazonaws.com/ohgiraffers_db?serverTimezone=Asia/Seoul
    username: ohgiraffers
    password: ohgiraffers

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect
        show_sql: true
